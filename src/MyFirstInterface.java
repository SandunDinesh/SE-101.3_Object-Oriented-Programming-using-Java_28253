public interface MyFirstInterface {
    public static final int x=0;  // In interface, there are no any difference between when we use public static final or not use it.Because in the interface, it already get variables as a public static final.
    int y=10; // This Answer is also 0.
    abstract void display();
    public void display1(); // In the Interface, there are no any difference between when we use abstract keyword or not.Because in interface, it already get method as a abstract methods.

}
